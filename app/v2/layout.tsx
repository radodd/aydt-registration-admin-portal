import "./portal-v2.css";

export const metadata = {
  title: "AYDT — Registration Portal (Design Preview)",
};

export default function V2Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
